apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: auth-middleware
  namespace: app
spec:
  forwardAuth:
    address: http://auth-svc.app.svc.cluster.local:80/auth/validate
    authResponseHeaders:
      - Authorization
      - X-User-Id
    trustForwardHeader: true
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-billing-prefix
  namespace: app
spec:
  stripPrefix:
    prefixes:
      - /billing
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-orders-prefix
  namespace: app
spec:
  stripPrefix:
    prefixes:
      - /orders
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-notifications-prefix
  namespace: app
spec:
  stripPrefix:
    prefixes:
      - /notifications
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: snackbar-router
  namespace: app
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`arch.homework`) && PathPrefix(`/auth`)
      kind: Rule
      services:
        - name: auth-svc
          namespace: app
          port: 80

    - match: Host(`arch.homework`) && PathPrefix(`/billing`)
      kind: Rule
      middlewares:
        - name: auth-middleware
          namespace: app
        - name: strip-billing-prefix
          namespace: app
      services:
        - name: billing-svc
          namespace: app
          port: 80

    - match: Host(`arch.homework`) && PathPrefix(`/orders`)
      kind: Rule
      middlewares:
        - name: auth-middleware
          namespace: app
        - name: strip-orders-prefix
          namespace: app
      services:
        - name: order-svc
          namespace: app
          port: 80

    - match: Host(`arch.homework`) && PathPrefix(`/notifications`)
      kind: Rule
      middlewares:
        - name: auth-middleware
          namespace: app
        - name: strip-notifications-prefix
          namespace: app
      services:
        - name: notification-svc
          namespace: app
          port: 80